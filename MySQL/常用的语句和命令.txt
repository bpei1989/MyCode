HeidiSQL可以在掌握常见SQL和理解数据库基本原理基础上使用，可以在一定程度上提高效率，因为其界面比较友好，比命令行查看容易些

登录
Mysql  -P 端口号  -h  mysql主机名\ip -u root （用户）  -p
如果是命令行是mysql所在的本机而且用默认的端口 3306 直接输入 mysql -u root -p 

创建数据库
CREATE DATABASE database-name 
删除数据库
drop database dbname

